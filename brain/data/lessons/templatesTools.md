# Lessons — templatesTools (Portal Templates Group)

## iats-modules-information — maintenance workflow

- **El endpoint `/repository/commits/:sha/diff` de GitLab capa `X-Total` en 1000 incluso paginando correctamente** (segunda capa del bug ya conocido de `get_commit_diff`, que trunca a 100 sin paginar). Para commits masivos (ej. batch PSR-4 de 30k+ líneas), "no aparece en las 1000 filas devueltas" NO es "0 archivos" — el path puede estar después del corte alfabético sin que la API señale truncamiento. Para conteos confiables de archivos tocados por módulo en commits masivos: comparación de árbol blob-a-blob (`list_tree` del commit vs. su padre). Documentado en el docstring de `gitlab_iats.py::get_commit_diff` (sección KNOWN CAPS, commit e72c5c27). Caso real y verificación completa: `brain/output/templatesTools/eval/stale_doc-batch-2026-08-15.md` (BLOCK de Smith por evidencia falsa "0 files" en 3 findings, corregida por Trinity con tree comparison: instance 11, uicore 1, user 47 archivos reales).
- **Una nota de resolución en `findings.jsonl` es evidencia permanente** — otras sesiones la leen como fuente de verdad para decidir qué está "ya revisado". Afirmar "revisé los N SHAs y ninguno toca el módulo" sin que sea cierto contamina el tracker peor que dejar el finding open. El gate de Smith re-verifica el texto de la nota contra GitLab en vivo, no solo los docs.
- **`resolve_findings.py --resolve <id>` permite corregir la nota de un finding ya resolved** (`resolve_finding()` en `tracker_store.py` no valida el status actual — sobrescribe la resolución). Es el camino correcto para enmendar evidencia, nunca editar `findings.jsonl` a mano.
- **`IATS_SKILL_MODE` puede estar seteada a `live` en el entorno de la shell** (seteada a nivel de proceso, no en dotfiles). Todo trabajo de mantenimiento (Flows C/D/E) requiere prefijar cada comando de la skill con `IATS_SKILL_MODE=docs` y verificar `mode_config.py` al inicio.

## codeReview — verificación de "no se abrió nada" y falsos verdes en suites

- **Un cero medido no es evidencia sin control positivo en la misma corrida.** Para probar que un flujo no abre browser/IDE: stub de `BROWSER` que loguea y **sale 0** (obligatorio, verificado contra fuente: `webbrowser.GenericBrowser.open` hace `return not p.wait()` y `webbrowser.open` itera `_tryorder` — si el stub sale ≠0 cae a `xdg-open` y abre Chrome real), stub `windsurf` en PATH para la rama IDE (`TERM_PROGRAM=vscode`), y un control positivo sin el mecanismo que dé >0 antes de confiar en el 0. Caso completo (diferencial 9→0, suites 72/0 y 92/2): `brain/output/templatesTools/eval/cr-no-open-gate.md`.
- **Un test que solo asevera exit 0 puede ser un falso verde.** `testCr.sh` G6_01 pasa con `-f "Config.nopage"` relativo: `cli.py` resuelve `-f` contra CWD → `Invalid Path` → nunca renderiza (el prepend del portal path existe en `testCrCli.sh`, no en `testCr.sh`). Verificado por Smith 2026-09-22 y arreglado el mismo día (commit `0b2ee1ea`: prepend del portal path a todo `-f` relativo en `testCr.sh`, espejo de `testCrCli.sh`, + guard que falla el test si el tool reporta `Invalid Path: File or folder not found` con `-f` presente, exento con `-p P_INVALID`). Gate independiente: prepend medido absoluto con logging de args, control negativo en copia scratch, suites 92/2 y 72/0. Límite conocido (Tier 1, sin fix): el guard no cubre la variante `Could not detect portal root from`.

## Repo/branch y rulesets (2026-09-25)

- **Verificá `git branch --show-current` antes de construir o commitear en este repo.** `shellScripts/python/toolsAutoUpdater.py` (disparado por `grefresh`, 1×/día vía `/var/tmp/toolslastupdate.txt`) hace `git checkout stable; git pull` sobre `templatesTools` y **no vuelve** al branch: una sesión que trabajaba en `T111111_...` quedó en `stable` a mitad de sesión (2026-09-25 07:36) y un subagente leyó el árbol equivocado. Si el branch no es el esperado, volvé al branch antes de tocar nada. Fix candidato (no hecho): que el updater preserve el branch actual. Detalle: `brain/output/templatesTools/plans/code-review-pending-priorities-2026-09-25.md`.
- **Un ruleset que referencia un id inexistente se omite en silencio** (`RulesManager._get_rules_by_rules_set` filtra por pertenencia a `RULES`): `views_missingStandardPlaceholders` estuvo referenciada sin definición y el set `views` corrió incompleto sin aviso. Antes de implementar la regla "faltante", medí si aplica: el espejo Twig `DEFAULT_*` daba 620 hallazgos en 147 themes reales (views usa `{!...}`) → se eliminó la clave fantasma. Chequeo mecánico candidato (no hecho): test que valide que toda clave de `RULES_SET` existe en `RULES`. Detalle: `brain/output/templatesTools/eval/code-review-pending-gate-2026-09-25.md`.

## Tokens y entorno (2026-09-25)

- **El token compartido de logs está hardcodeado en 3 lugares, no en 1** (verificado por hash el 2026-09-25: mismo valor): `codeReview/python/core/_gitlab_token.py` (removido en `d753a2bd`), `shellScripts/python/Gitlab.py::setLoggerToken()` y `aiScripts/cognition/skills/shared/scripts/log_skill_usage.py::LOGGER_TOKEN`. Remover una copia no elimina la fuga: antes de declarar "token resuelto", grepear el valor en todo el repo (`grep -rl` por el prefijo `glpat-`). El comentario de `log_skill_usage.py` afirma que los tokens personales no pueden pushear a master de `templateslogs`; el E2E con `PRIVATE_TOKEN` sí pudo (acceso Maintainer vía grupo), así que ese claim hay que re-verificarlo antes de reusarlo. Decisión del dueño pendiente sobre los 2 restantes.
- **Una variable definida en los dotfiles no llega a los procesos hijos si no está exportada.** `~/.bash_aliases` define `PRIVATE_TOKEN` sin `export`; el tool Python lee `os.environ`, así que no la veía (y el fallback hardcodeado lo tapaba). Los dotfiles sourcean todos los `.sh` de `shellScripts/bash/`, por eso el export top-level guardado en `helpers.sh` (commit `d753a2bd`) lo resuelve para todos los caminos sin acción del usuario. Regla: cuando un tool necesita una variable de los dotfiles, verificá que esté **exportada** (no sólo seteada) en el camino real de invocación — `bash -c 'source ~/.bash_aliases; env | grep -c ^VAR='`.


